#include<stdio.h>
void main()
{
  int a,count;
  printf("enter a number");
  scanf("%d",&a);
  for(int i=2;i<a;i++)
  {
      if(a%i==0)
      {
          count++;
      }
  }
  if(count==0)
  printf("it is prime");
  else
  printf("it is composite");
  }
  
  
